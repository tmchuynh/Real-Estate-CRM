☐ Update all static data to queried data
☐ Update handleDetailsClick from Leads.jsx
☐ Add POST route for adding Lead Event
☐ Add Gatekeeping for all routes that aren't login/reg via the /api/users/authorization Express route that checks if user in session
☐ When User is queried to pass to React, decode profile Pic back to img file